[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-redteam-platform"
version = "0.1.0"
description = "Authorized AI red-team control plane: registry, safe runner, scoring, evidence, compliance, reports."
license = { text = "Apache-2.0" }
requires-python = ">=3.11"

[tool.setuptools]
packages = ["ai_redteam_platform"]
