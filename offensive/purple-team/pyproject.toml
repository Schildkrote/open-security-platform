[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "purple-team"
version = "0.1.0"
description = "ATT&CK-based detection coverage, safe testing, and gap analysis."
license = { text = "Apache-2.0" }
requires-python = ">=3.11"

[tool.setuptools]
packages = ["purple_team"]
