[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-redteam-range"
version = "0.1.0"
description = "Intentionally vulnerable-by-design AI apps for authorized red-team testing and training."
license = { text = "Apache-2.0" }
requires-python = ">=3.11"

[tool.setuptools]
packages = ["range_apps"]
