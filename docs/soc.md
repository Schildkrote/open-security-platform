# SOC

Security operations and incident response automation.

| Component | Lang | Description |
|---|---|---|
| `soc/open-soar` | Node/TS | SOAR / IR automation: playbook DAG engine (parallel branches, dependency ordering), case management, enrichment, response actions, AI-assisted triage, and a hash-chained audit trail. |

See the component's `README.md` and `NEXT_STEPS.md` in the repository.
