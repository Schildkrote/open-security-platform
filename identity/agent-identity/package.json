{
  "name": "agent-identity",
  "version": "0.1.0",
  "description": "Open Agent Identity & Permissions: agent registry, scoped tokens, delegation chains, JIT permissions, approvals, revocation, agent-to-agent auth.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node --experimental-strip-types --no-warnings src/server.ts",
    "test": "node --experimental-strip-types --no-warnings --test test/unit.test.ts test/integration.test.ts"
  },
  "engines": { "node": ">=22.6" },
  "license": "AGPL-3.0-only"
}
